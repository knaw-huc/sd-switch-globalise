version: '2'

services:
  gl-switch:
    build: .
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.sd-editor.loadbalancer.server.port=8080"
      - "traefik.http.routers.sd-editor.entrypoints=http"
      - "traefik.http.routers.sd-editor.rule=Host(`resolve.sd.di.huc.knaw.nl`)"
    networks:
      - traefik-public
networks:
  traefik-public:
    external:
      name: traefik-public
